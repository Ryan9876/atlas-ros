name: v610-promote-once-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Retired controller notice
        run: echo "Retired after successful Atlas ROS v6.1.0 publication, restoration, tag verification, and live-authority readback."
