# ADR-001: YAML for finite policy configuration

Status: Accepted. YAML provides readable reviewed configuration for finite policy values while Pydantic and deterministic rules supply validation. Markdown retains rationale; Python retains behavior; YAML does not contain secrets or release-specific runtime IDs.

