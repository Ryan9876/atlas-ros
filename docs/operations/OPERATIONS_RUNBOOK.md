# Operations runbook

Initialize local runtime with `atlas initialize`; inspect with `atlas status`; create protected pending captures with `atlas capture TEXT`; assess an Action with `atlas decompose`; generate a no-write execution plan with `atlas todoist plan`; and verify release contents using `atlas release checksums` followed by `atlas release verify`. All candidate commands remain non-production without a separately configured adapter and authorization.

