# v8.3.0 Exact Package Promotion Authorization Template

Use only after the retained candidate artifact and independent verification evidence replace every `PENDING` value.

> I authorize publication and controlled production activation of Atlas ROS v8.3.0 from candidate commit `PENDING`, retained artifact `PENDING` with SHA-256 `PENDING`, source distribution SHA-256 `PENDING`, and wheel SHA-256 `PENDING`. I authorize the exact bounded-autonomy policy `PENDING`, beginning in `MONITOR_ONLY`, and the documented activation sequence only after the approved HTTPS runtime, secret model, Todoist OAuth/webhook setup, Notion webhook verification, retention/backup ownership, zero-write parity validation, rollback proof, governing Decision, Acceptance Review, and final readback are complete. I do not authorize rebuilding the package, restoring or writing W04, expanding integration scope beyond the accepted inventory, or bypassing the documented approval/block matrix.
