# Atlas ROS v8.2.0 Exact Package Authorization

Status: **template — not authorized**

Ryan authorizes only the exact package and actions populated below after independent review.

- Source commit: `<EXACT_COMMIT>`
- Retained candidate artifact ID: `<ARTIFACT_ID>`
- Candidate artifact SHA-256: `<ARTIFACT_SHA256>`
- Source distribution SHA-256: `<SDIST_SHA256>`
- Wheel SHA-256: `<WHEEL_SHA256>`
- SPDX SBOM SHA-256: `<SBOM_SHA256>`
- Validation receipt SHA-256: `<VALIDATION_SHA256>`
- Natural-comment evidence SHA-256: `<EVIDENCE_SHA256>`
- Source-tree SHA-256: `<SOURCE_TREE_SHA256>`
- Tests and coverage: `<TEST_RESULT>` / `<COVERAGE>`
- Active restoration: `<ACTIVE_VERSION>` at `<ACTIVE_COMMIT>`
- Immediate rollback restoration: `<ROLLBACK_VERSION>` at `<ROLLBACK_COMMIT>`
- Production Notion migration: none; schema changes 0

Authorized actions must be enumerated explicitly. Unlisted actions remain unauthorized. Publication must use the retained artifacts without rebuilding. Authority activation must occur only after independent publication readback and no-migration verification.
