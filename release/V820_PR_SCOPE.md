# v8.2.0 draft pull-request scope

This branch contains only a checksum-bound development bootstrap until the draft pull request is opened. The bootstrap materializes the exact v8.2.0 source change, removes all temporary transfer files, and commits the normal source tree. It performs no production provider, release, migration, tag, merge, or authority action.
