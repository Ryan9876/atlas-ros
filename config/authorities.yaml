version: 1
sources:
  drive: {owns: [release_policy, manifests, checksums, rollback]}
  notion: {owns: [system_state, captures, actions, delegated_work, projects, risks, operations, decisions, integrations, automations, reviews]}
  todoist: {owns: [task_completion, priority, due_date, project, section, labels, task_tree]}
  sqlite: {owns: [pending_capture, outbox, retry, cache, locks], non_authoritative: true}
prohibited: [outlook_email, outlook_calendar, autonomous_execution, calendar]

