# Security policy

Secrets remain in approved connector storage or macOS Keychain. SQLite, observability, fixtures, source distributions, and release artifacts must not contain credentials, authorization headers, or raw capture content. Unknown observability fields are rejected. Production promotion, destructive actions, autonomous execution, Calendar, and Outlook integrations are prohibited by candidate policy.

